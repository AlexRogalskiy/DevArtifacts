* the progress bar on the dcc window goes to 100% momentarily before dropping back down to the correct length
* if 'refresh' is clicked twice in the channel list, it will send two LIST requests
  and add all the results to the tree. Should probably set the refresh button inactive
  until at least some results come in.
* high-speed DCC transfers tend to make the whole application crap out. Other
  DCCs die, lag becomes astronomical. This is almost certainly a real xchat
  bug
* nick completion segfaults overwrites text if the cursor isn't at the end of
  the entry when completing
