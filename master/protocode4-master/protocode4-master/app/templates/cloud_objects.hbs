<div class='col-md-3 items-sidebar'>
    <div class="items-panel">
        <div class='items-title'>OBJECTS</div>
        <hr>
        <ul class="items-list">
            {{#each item in controller item-controller="cloudObjectsController" }}
                {{#link-to "cloud_object" item}}
                    <li class="items-list-item">
                        <i class="fa fa-dot-circle-o"></i>{{item.name}}
                    </li>
                {{/link-to}}
            {{/each}}
            {{#if isCreating}}
                <li class="items-list-edit">
                    <div class='col-md-1'></div>
                    <div class="input-group col-md-10">
                        {{focus-input value=objectName class="form-control input-sm"}}
                        <span class="input-group-btn">
                      <button type="button" class="btn btn-default btn-sm" {{action "createCloudObject"}}>
                          <span class="glyphicon glyphicon-floppy-disk"></span>
                      </button>
                      <button type="button" class="btn btn-default btn-sm" {{action "setCreating" false}}>
                          <span class="glyphicon glyphicon-remove"></span>
                      </button>
                  </span>
                    </div>
                    <div class='col-md-1'></div>
                </li>
            {{/if}}
        </ul>
        <div class="panel-footer panel-buttons">
            <div class="btn-group">
                <button type="button" id="button-add" class="btn btn-default btn-sm" {{action "setCreating" true}}>
                    <i class="fa fa-plus"></i>
                </button>
            </div>
        </div>
    </div>
</div>
<div class='col-md-9' id='entity-right-panel'>
    {{outlet}}
</div>
