Функция Abs(А)
	Возврат ?(А >= 0, А, -А);
КонецФункции

Процедура ПечатьДоски(доска)
	Перем i, s;
	s = "";
	Для i = 0 По 7 Цикл
		s = s + " " + доска[i];
	КонецЦикла;
	Сообщить(s);
КонецПроцедуры

Процедура РешениеЗадачиОВосьмиФерзях()
	Перем доска, i, j, подАтакой;
	доска = Новый Массив(8);
	i = 1;
	Пока Истина Цикл
		доска[i - 1] = 1;
		Пока Истина Цикл
			подАтакой = Ложь;
			Если i <> 1 Тогда
				Для j = 1 По i - 1 Цикл
					Если (доска[i - 1] = доска[j - 1]) Или (Abs(доска[i - 1] - доска[j - 1]) = Abs(i - j)) Тогда
						подАтакой = Истина;
						Прервать;
					КонецЕсли;
				КонецЦикла;
			ИначеЕсли доска[i - 1] >= 5 Тогда
				Возврат;
			КонецЕсли;
			Если Не подАтакой Тогда
				i = i + 1;
				Если i <= 8 Тогда
					Прервать;
				КонецЕсли;
				ПечатьДоски(доска);
				i = 8;
			КонецЕсли;
			Пока доска[i - 1] >= 8 Цикл
				i = i - 1;
			КонецЦикла;
			доска[i - 1] = доска[i - 1] + 1;
		КонецЦикла;
	КонецЦикла;
КонецПроцедуры

РешениеЗадачиОВосьмиФерзях();

конец программы