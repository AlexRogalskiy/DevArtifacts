jenkins-restnotifier
====================

Simple jenkins plugin that notifies a configured URL when the build starts and finishes.

Allows to configure a separate URL for each build result and  can optionally pass the result
of the previous build as a parameter to the URL that gets called on build start.
