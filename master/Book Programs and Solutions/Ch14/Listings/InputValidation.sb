' InputValidation.sb

num = -1   ' Invalid value (to force a pass through the loop)

While ((num < 1) Or (num > 5))
  TextWindow.Write("Enter a number between 1 and 5: ")
  num = TextWindow.ReadNumber()
EndWhile
TextWindow.WriteLine("You entered: " + num)