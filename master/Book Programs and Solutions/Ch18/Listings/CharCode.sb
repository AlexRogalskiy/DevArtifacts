' CharCode.sb
str = "ABab12"
For N = 1 to Text.GetLength(str)
  ch = Text.GetSubText(str, N, 1)         ' Gets the Nth character
  code = Text.GetCharacterCode(ch)        ' Gets its code point
  TextWindow.WriteLine(ch + ": " + code)  ' Displays ch and its code point
EndFor