' TryItOut_8_4.sb

TextWindow.Write("Enter the value of x: ")
x = TextWindow.ReadNumber()

TextWindow.Write("Enter the value of y: ")
y = TextWindow.ReadNumber()

If (x > 5) Then
  If (y > 5) Then
    TextWindow.WriteLine("The skylight is falling!")
  Else
    TextWindow.WriteLine("Now it's time to play the piper!")
  EndIf
Else
  TextWindow.WriteLine("I'll huff, puff, and blow $5 on tacos!")
EndIf