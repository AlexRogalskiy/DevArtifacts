' SquareRootDemo.sb
TextWindow.Write("Enter the length of side 1: ")
s1 = TextWindow.ReadNumber()

TextWindow.Write("Enter the length of side 2: ")
s2 = TextWindow.ReadNumber()

hypot = Math.SquareRoot(s1 * s1 + s2 * s2)
TextWindow.WriteLine("Hypotenuse = " + hypot)