' TryItOut_19_1.sb

' Change these paths to your own
inFile = "C:\Temp\Test1.txt"
outFile = "C:\Temp\Out.txt"

' Reads the input file
strIn = File.ReadContents(inFile)

' Converts the text to lowercase
strOut = Text.ConvertToLowerCase(strIn)

' Saves the result to the output file
File.WriteContents(outFile, strOut)