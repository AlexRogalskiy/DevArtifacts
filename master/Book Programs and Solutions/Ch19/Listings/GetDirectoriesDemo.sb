' GetDirectoriesDemo.sb
path = "D:\Temp"
dirArray = File.GetDirectories(path)
count = Array.GetItemCount(dirArray)
TextWindow.WriteLine(path + " contains " + count + " directories:")
For N = 1 to count                        ' Displays the array's elements
  TextWindow.WriteLine(" " + dirArray[N])
EndFor