' AndDemo.sb
TextWindow.Write("Game level: ")
gameLevel = TextWindow.ReadNumber()

TextWindow.Write("Score.....: ")
score = TextWindow.ReadNumber()

If ((gameLevel = 1) And (score > 100)) Then
  TextWindow.WriteLine("You get 200 bonus points!")
EndIf  