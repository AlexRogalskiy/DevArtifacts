<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout"
         xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <f:section title="JenkinsREST">
    <f:hetero-list name="notifiers" hasHeader="true"
                   descriptors="${it.notifierClasses}" items="${it.restNotifiers}"
                   addCaption="add notifier" deleteCaption="delete notifier"
    />
  </f:section>
</j:jelly>