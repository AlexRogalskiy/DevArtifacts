{% extends 'base.html.twig' %}

{% block content %}

    <div class="container">

        <h1>Registration</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            Submit
        </button>

        {{ form_end(form) }}

    </div>

{% endblock %}